<div id="addSoilDetails">
	<h2 class="sideHeading">Add Soil Details</h2>
	<%= form_for(@newsoil) do |fields|%>
		<%= render "newSoilFields", f:fields%>
		<div class="button_align">
			<ul>
				<li><%= fields.submit "Submit", class: 'submit_button' %></li>
				<li><%= link_to "Cancel", soils_path(search: selected_farm.id), class: 'submit_button' %></li>
			</ul>
		</div>	
	<% end%>
</div>